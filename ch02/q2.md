```javascript
// Coded solution to question 2

/**
 * 2. What is wrong with the statement
 * 
 *      if(x === 0) console.log('zero') else console.log('non zero')
 * 
 *    How do you fix the problem?
 */

let x;

if(x === 0) console.log('zero'); else console.log('non zero')      // insert a semi-colon before the else keyword
```
